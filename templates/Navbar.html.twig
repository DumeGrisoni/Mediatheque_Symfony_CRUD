<nav class="navbar navbar-expand-lg navbar-light nav_obj sticky-top" style="height: 75px">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('catalogue') }}">
            <img src="{{ asset('/Images/Logo.png') }}" alt="Logo du site" style="height: 70px">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto mx-4">
                <li class="nav-item mx-4">
                    <a class="nav-link" aria-current="page" href="{{ path('catalogue') }}" id="nav-a">Catalogue</a>
                </li>
                <li class="nav-item mx-4">
                    {% if is_granted('ROLE_EMPLOYE') %}
                        <a class="nav-link" aria-current="page" href="{{ path('admin') }}" id="nav-a">Administration</a>
                        {% else %}
                            <a class="nav-link" aria-current="page" href="{{ path('espace_index') }}" id="nav-a">Espace privé</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>

<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>