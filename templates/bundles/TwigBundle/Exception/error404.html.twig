{% extends 'base.html.twig' %}
{% block title %} Médiathèque | Page non trouvée {% endblock %}

{% block body %}
    <div class="container-md main-title w-50 mt-5" >
        <div class="text-center row mb-5 ">
            <div class="col mt-5">
                <h1 style="font-family: var(--title-font); font-size: var(--title-size-big)"> La page n'a pas été trouvée</h1>
            </div>
        </div>
        <div class="row text-center mt-5">
            <div class="col mt-5">
                <p style="font-size: var(--title-size-h1)">
                    Peut-être une erreur de frappe lors de votre recherche ?
                </p>
            </div>
        </div>
        <div class="row text-center mt-5">
            <div class="col mt-5">
                <button class="button rounded w-25">
                    <a href="{{ path('Home') }}"><p style="font-size: var(--title-size-h2)">Retourner à l'acceuil</p></a>
                </button>
            </div>
        </div>
    </div>
{% endblock %}